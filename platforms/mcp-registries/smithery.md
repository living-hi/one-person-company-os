# Smithery Submission

Package: One Person Company OS

Version: 1.0.2

Use the MCP server package:

- `platforms/mcp-server/server.py`
- `platforms/mcp-server/server.json`
- `platforms/mcp-server/README.md`

Listing summary:

Build a visual operating cockpit for an AI-native one-person company through a local MCP server. The server exposes tools for preflight checks, workspace initialization, focus updates, artifact generation, and release validation.

Operational boundary:

- stdio MCP server
- no automatic system package installation
- persistent writes only inside the founder-approved workspace
- high-risk pricing, legal, launch, budget, and customer-facing actions require approval

Submission blocker:

Smithery account/API key or compatible hosted endpoint.

Published evidence:

Smithery listing URL or API record for version `1.0.2`.
